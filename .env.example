# ========= 开发环境（vite dev） =========
# 留空 → 前端用相对路径 /api/*，由 vite dev server 代理转发到后端，天然无跨域
VITE_API_BASE=

# vite dev server 的代理目标（不会打进 bundle）
VITE_PROXY_TARGET=http://localhost:8080

# ========= 生产构建（vite build） =========
# 若前端与后端同源部署（推荐，通过反向代理），保持留空
# 若前端独立部署（如 Vercel），填后端完整地址，并确保后端 CORS 已加白名单
# VITE_API_BASE=http://localhost:8080
